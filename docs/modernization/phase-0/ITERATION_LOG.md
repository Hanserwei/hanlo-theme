# 阶段 0 迭代日志

## P0-I0：执行骨架与静态盘点

### 基本信息

| 字段 | 值 |
| --- | --- |
| 状态 | 已完成 |
| 日期 | 2026-08-31 |
| 基准提交 | `ac3d02bf` |
| 变更类型 | 文档与只读静态盘点 |
| 运行行为变化 | 无 |

### 本轮完成

- 将路线图阶段 0 标记为“进行中”。
- 新建阶段 0 文档入口、执行计划、功能清单、决策表和回归基线规范。
- 将八项路线图任务拆成 P0-I0 至 P0-I6 七轮迭代。
- 完成核心功能、特殊页面、侧栏、评论、全局交互和候选删除项的代码映射。
- 区分侧栏爱发电与文章/关于页打赏，避免把两类能力当作同一删除项。
- 确认当前仓库未找到明确的群聊实现，并把“推广”定义列为阻塞决策。
- 记录当前模板、脚本、样式、静态资源、vendor 和主题 ZIP 静态数据。
- 记录朋友圈配置不闭环、图库缺失设置、DPlayer/HLS 疑似未使用、音乐资源全站加载等问题。

### 证据摘要

| 证据 | 结果 |
| --- | --- |
| HTML/JS/CSS 统计 | 107 / 48 / 25 |
| `templates/` 文件数 | 234 |
| 主题静态资源 | 130 文件 / 6,598,813 bytes |
| 本地 vendor | 45 文件 / 2,234,834 bytes |
| 当前 ZIP | 4,008,711 bytes |
| 已知全站加载本地集合 | 28 文件 / 897,009 bytes（未压缩静态估算） |
| 工作树初始状态 | 干净 |

### 本轮没有做

- 没有删除广告、赞助、评论或任何资源。
- 没有访问或修改运行中的 Halo 实例。
- 没有采集真实浏览器截图、HAR 或性能数据。
- 没有确定评论系统、浏览器范围和最终可选功能清单。

### 风险与发现

1. “赞助”至少包含两套相互独立的功能，必须分开决策。
2. 音乐又分音乐馆、左下角播放器、侧栏卡片三套入口，不能只用一个“保留音乐”结论。
3. 评论 provider 影响评论区、最近评论、弹幕和多个特殊页面，删除时必须跨文件审计。
4. 当前静态估算显示大量第三方资源全站加载，但真实传输数据仍需浏览器验证。
5. 基线截图依赖稳定 Halo 环境、固定内容和插件组合，目前尚未提供。

### 下一轮入口

维护者已完成产品范围、兼容范围和本地环境确认，进入 P0-I1。

---

## P0-I1：产品决策与兼容契约

### 基本信息

| 字段 | 值 |
| --- | --- |
| 状态 | 已完成 |
| 日期 | 2026-08-31 |
| 关联决策 | P0-DEC-001 至 P0-DEC-016 |
| 运行行为变化 | 无；仅将主题声明最低版本提高到 `>=2.26.0` |

### 已接受结论

- 删除 Google AdSense、自定义广告、侧栏爱发电、微信公众号组件和 51LA。
- 只保留侧栏音乐卡片，删除音乐馆与左下角导航播放器。
- 评论只保留 Halo `plugin-comment-widget`，删除 Twikoo、Artalk、Waline及其专属功能。
- 保留友链、互动友链、开往、3D 分类、自定义右键菜单和微信/支付宝直接打赏。
- 最低 Halo 版本为 `2.26.0`。
- 浏览器只支持桌面与 Android 手机的 Chrome/Chromium 当前稳定版及前一个大版本。
- 使用 `http://localhost:8090/` 作为阶段 0 测试环境，截图证据以压缩 WebP 随 Git 保存。

### 下一轮入口

使用 Halo CLI 只读盘点本地环境，并建立删除前冒烟证据。

---

## P0-I2：删除前最小冒烟基线

### 基本信息

| 字段 | 值 |
| --- | --- |
| 状态 | 已完成 |
| 日期 | 2026-08-31 |
| 环境 | `http://localhost:8090/`，Halo `2.26.0-SNAPSHOT` |
| 浏览器 | Google Chrome `152.0.7977.64` |
| 运行行为变化 | 无 |

### 完成内容

- 通过 Halo CLI 0.5.0 使用 PAT 完成主题、插件、配置、文章、页面、分类、标签和评论只读盘点。
- 固定首页、文章、独立页面、分类、标签和留言板六条核心路由。
- 为六条路由各采集 1440×900 桌面和 390×844 手机浅色视口截图，共 12 张 WebP。
- 验证移动菜单打开后 `sidebar-menus.open=true` 且锁定页面滚动；关闭后状态和滚动恢复。
- 连续执行 10 次 PJAX 导航，全部收到 `pjax:complete`，页面路径与标题正确，最终只存在一个 `#nav` 和一个 `#body-wrap`。
- Chrome 日志没有稳定复现的控制台异常、JavaScript 异常或失败网络请求。

### 已知基线问题

- 当前运行时主题声明仍为 `>=2.22.1`，源码已调整为 `>=2.26.0`，将在裁剪后的本地部署验证中同步。
- Halo 报告主题缺少可选的 `templates/layout.html` 页面布局契约，状态为 `MissingLayoutTemplate`；这是本轮前已存在的问题。
- `#loading-box` 完成后保留 `loaded` 类且计算样式为可见，但截图中没有遮挡页面；本轮视为现有实现细节。

### 证据

- 环境与路由：[`LOCAL_ENVIRONMENT.md`](LOCAL_ENVIRONMENT.md)
- 截图及 SHA-256：[`evidence/pre-removal/README.md`](evidence/pre-removal/README.md)

### 下一轮入口

进入 P0-I3，按决策边界实施功能裁剪。

---

## P0-I3：分批删除已废弃功能

### 基本信息

| 字段 | 值 |
| --- | --- |
| 状态 | 已完成 |
| 日期 | 2026-08-31 |
| 关联决策 | P0-DEC-003、004、006、008、009、012 |
| 本地环境 | `http://localhost:8090/` |
| 回退备份 | `/home/hanserwei/halo-project/.halo-backups/hanlo-phase0-20260831-pre-prune/` |

### 实际变更

- 删除 Google AdSense、自定义广告的侧栏选项、配置、模板和样式。
- 删除侧栏爱发电、爱发电 Finder/API 分支、赞助列表和专属图片；保留微信/支付宝直接打赏。
- 删除微信公众号侧栏组件、配置、模板、样式和专属图片；保留文章同步状态显示。
- 删除全站与关于页 51LA 请求、配置、模板和样式；关于页继续保留地图与作者信息。
- 删除音乐馆自定义页面、左下角导航播放器、APlayer/Meting 资源和播放器控制代码；保留侧栏音乐卡片。
- 删除 Twikoo、Artalk、Waline 的配置、模板、脚本、样式和 vendor 文件，只保留 Halo 原生评论。
- 删除只服务于外部评论的匿名信息、评论弹幕、表情放大和右键引用功能。
- 保留并验证友链、开往、3D 分类、自定义右键菜单和侧栏音乐卡片代码落点。
- 将主题最低 Halo 版本声明更新为 `>=2.26.0`。

### 删除的专属文件

- 9 个 HTML 模板或片段。
- 9 个 JavaScript 文件。
- 4 个 CSS 文件。
- 4 个二进制图片资源。
- 7 个清空后的功能目录。

### 验证结果

| 检查 | 结果 |
| --- | --- |
| YAML 解析 | `theme.yaml`、`settings.yaml`、5 个 AnnotationSetting 文档全部通过 |
| JavaScript 语法 | 修改的 5 个业务脚本通过 `node --check` |
| 删除项残留扫描 | 设置、模板和非共享代码中无命中 |
| 主题运行状态 | 重载后 `READY=TRUE`，最低版本为 `>=2.26.0` |
| 页面 HTTP | 9 条核心/附加路由全部返回 200 |
| 设置 Schema | 不再包含广告、爱发电、公众号、51LA、外部评论和导航音乐字段 |
| Halo 原生评论 | 文章页加载 PluginCommentWidget 3.2.2，未加载外部 provider 资源 |
| 保留功能 | 首页侧栏音乐卡片存在，右键菜单可显示，友链/开往/3D 分类代码仍在 |
| PJAX | 连续 10 次导航均完成，路径和标题正确，DOM 无重复 `#nav`/`#body-wrap` |
| 裁剪后截图 | 12 张，桌面/手机各 6 张 |
| 主题 ZIP | 2,353,796 bytes，压缩数据校验通过 |

### 已知问题

- 快速连续 PJAX 导航偶发触发远程 Typed.js 对已离开页面节点的 `tagName` 空引用；这是统一生命周期前的既有风险，留给阶段 2 处理。
- Halo 的 `MissingLayoutTemplate` 状态仍存在，与本轮裁剪无关。
- 运行时 ConfigMap 可能保留旧字段值，但 Setting Schema 和所有模板引用已经删除；不对用户配置执行破坏性全量替换。
- 共享 IconFont 中仍可能包含历史爱发电或微信图标字形；它们与大量保留图标共用字体文件，本轮不重建共享字库。

### 恢复方式

运行时主题完整备份和脱敏配置已保存在上述回退目录并附 SHA-256。需要回退时应恢复整个运行时主题目录后重载主题，不能只恢复单个模板。

### 下一轮入口

进入 P0-I4，固定最终配置场景、动态内容处理规则和 P0-I5 完整浅色/深色基线。

---

## P0-I4：固化测试场景与数据

### 基本信息

| 字段 | 值 |
| --- | --- |
| 状态 | 已完成 |
| 日期 | 2026-08-31 |
| 环境 | `http://localhost:8090/` |
| 交付物 | [`TEST_SCENARIOS.md`](TEST_SCENARIOS.md) |

### 完成内容

- 将六条核心路由和三条附加路由固定为阶段 0 长期样本。
- 将“React 对照速成文档”固定为代码块、表格、目录和评论样本。
- 定义 S0 无可选插件、S1 最小评论、S2 保留插件全开、S3 原生评论数据四类场景。
- 记录 14 个本地插件及其版本，明确哪些参与前台基线。
- 定义浅色/深色、桌面/手机、localStorage、动画和随机内容的稳定规则。
- 记录脱敏主题配置 SHA-256，不提交 PAT、Cookie 或完整敏感配置。

### 下一轮入口

按 S2 采集 24 张正式浅色/深色核心页面截图，再切换到 S0/S1 验证插件缺失和最小组合。

---

## P0-I5：最终行为与视觉基线

### 基本信息

| 字段 | 值 |
| --- | --- |
| 状态 | 已完成 |
| 日期 | 2026-08-31 |
| 浏览器 | Google Chrome 152.0.7977.64 |
| 证据 | [`evidence/final/README.md`](evidence/final/README.md) |

### 完成内容

- S2 场景下为六条核心路由采集桌面/手机、浅色/深色 24 张正式截图。
- S0 场景下为首页、文章和留言板采集桌面/手机、浅色/深色 12 张安全降级截图。
- S0 的九个前台可选插件均成功停用；核心与附加路由保持 HTTP 200，页面不加载评论插件资源，也不显示空评论入口。
- S1 只启用 `PluginCommentWidget` 后，文章、留言板和最近评论均返回 200，原生评论资源正常加载。
- 测试结束后九个前台插件全部恢复为 `STARTED`，本地环境回到 S2。
- 代表文章识别出 42 个 Shiki 代码块、42 个复制按钮和 42 个折叠按钮；折叠状态切换通过。
- 自定义右键菜单显示通过；移动菜单打开/关闭与页面滚动锁恢复通过。
- 浏览器后退从标签页回到分类页，前进再回到标签页，路径恢复正确。
- 正常节奏的页面加载和导航没有 JavaScript 异常。

### 已知基线噪声

- 桌面页面会请求相对路径 `null` 并返回 404，来源是现有 Fancybox CSS 占位配置 `/null`；手机视口没有稳定触发。阶段 0 将其记录为现有问题，不混入本轮功能裁剪。
- 无等待的快速 PJAX 压力导航可能触发远程 Typed.js 对已离开节点的空引用，已归入阶段 2 生命周期治理。
- 首页打字、瞬间轮播、粒子与动态文案已在截图前暂停动画，但动态内容本身仍是允许差异区域。

### 下一轮入口

进入 P0-I6，采集首页、文章和留言板的首次加载、缓存刷新与 PJAX 资源数据，完成最终残留和验收审计。

---

## P0-I6：资源基线与阶段审计

### 基本信息

| 字段 | 值 |
| --- | --- |
| 状态 | 已完成（待提交） |
| 日期 | 2026-08-31 |
| 网络证据 | [`evidence/final/RESOURCE_BASELINE.json`](evidence/final/RESOURCE_BASELINE.json) |
| 完成审计 | [`COMPLETION_AUDIT.md`](COMPLETION_AUDIT.md) |

### 完成内容

- 使用 Chrome DevTools Protocol 采集首页、文章和留言板的冷加载、缓存刷新和 PJAX 九组资源数据。
- 记录请求、传输字节、解码字节、JS、CSS、图片、字体、第三方域名和失败请求。
- 用临时 Git index 构建最终 ZIP：2,353,855 bytes、211 个文件，压缩数据校验通过。
- 通过 Halo 主题升级 API 上传该 ZIP；返回 200，主题为 `READY=TRUE`，最低版本声明为 `>=2.26.0`。
- 用相同值回写 `style.colorScheme`，验证主题设置保存往返通过。
- 完成 YAML、JavaScript、Markdown 链接、功能残留、运行时页面和插件恢复审计。

### 资源发现

- 首页冷加载 148 个请求、约 22.15 MB，主要由约 21.57 MB 图片构成。
- 文章冷加载 147 个请求、约 13.31 MB；99 个脚本请求与运行时 Shiki 完整包是后续重点。
- 留言板冷加载 129 个请求、约 1.67 MB。
- 外部资源域名较多，缓存命中较少；这些问题已映射到阶段 4 和阶段 6。

### 已知失败请求

- 首页和文章桌面场景存在相对 `null` 路径 404，来自现有 Fancybox CSS 占位。
- 留言板两个头像请求被 Chrome ORB 拦截，来源是当前评论头像 URL，而非本轮裁剪资源。

### 阶段状态

所有工作树内验收项已通过。根据路线图维护约定，阶段 0 在当前变更提交或合并到 `master` 前仍保持“进行中”。

---

## 后续迭代记录模板

复制以下结构并替换编号：

```markdown
## P0-Ix：标题

### 基本信息

| 字段 | 值 |
| --- | --- |
| 状态 | 进行中 / 已完成 / 暂停 |
| 日期 | YYYY-MM-DD |
| 分支/PR/提交 |  |
| 关联决策 | P0-DEC-xxx |
| 运行行为变化 |  |

### 本轮目标

- [ ]

### 实际变更

-

### 验证证据

| 检查 | 命令/环境 | 结果 |
| --- | --- | --- |
| 静态残留 |  |  |
| 打包 |  |  |
| Halo 安装/升级 |  |  |
| 页面冒烟 |  |  |
| 截图/网络记录 |  |  |

### 已知风险和未完成项

-

### 回退方式

-

### 下一轮入口

-
```
